// No longer needed — cloudflared binary download removed.
// KonnectBridge now uses a persistent WebSocket relay instead.
console.log('[Bridge] Binary download script is disabled (relay mode active).');
