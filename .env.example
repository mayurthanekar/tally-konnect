# Konnect History API (configure per your Konnect provider)
# Example: https://api.konnect.network or https://sandbox.konnect.network
KONNECT_BASE_URL=https://api.konnect.network
# API key / Bearer token for Konnect (required if API is authenticated)
KONNECT_API_KEY=

# Optional: Konnect History endpoint path (default: /v1/transactions or /payments/history)
# Query params fromDate & toDate (ISO 8601) are appended by the app
KONNECT_HISTORY_PATH=/v1/transactions

# Tally – On-Prem (TallyPrime/ERP listening on port 9000) and optional Cloud ERP
TALLY_URL=http://localhost:9000
# Optional: default Cloud ERP URL for the web app (user can override in UI)
TALLY_CLOUD_URL=

# Ledger name in Tally for Konnect transactions (create this ledger under Bank Accounts or Current Assets)
TALLY_KONNECT_LEDGER=Konnect Bank
# Other side of entry for Receipt/Payment (e.g. Cash, or a generic "Sundry Debtors" for customer payments)
TALLY_DEFAULT_CREDIT_LEDGER=Cash
